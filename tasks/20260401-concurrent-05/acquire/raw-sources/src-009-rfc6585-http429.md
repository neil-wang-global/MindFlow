# Raw Source

## Source ID
- src-009

## ACQ Event
- ACQ-003

## Source URL
- https://datatracker.ietf.org/doc/html/rfc6585

## Fetch Timestamp
- 2026-04-01T10:24:00Z

## Source Type
- Technical Documentation

## Credibility Assessment
- High
- IETF RFC — defines HTTP 429 status code

## Fetch Status
- success

## Fetch Completeness
- complete
- RFC 6585 is a short document

## Original Content

RFC 6585 - Additional HTTP Status Codes

Abstract

This document specifies additional HyperText Transfer Protocol (HTTP) status codes for a variety of common situations.

4. 429 Too Many Requests

The 429 status code indicates that the user has sent too many requests in a given amount of time ("rate limiting").

The response representations SHOULD include details explaining the condition, and MAY include a Retry-After header indicating how long to wait before making a new request.

For example:

HTTP/1.1 429 Too Many Requests
Content-Type: text/html
Retry-After: 3600

<html>
   <head>
      <title>Too Many Requests</title>
   </head>
   <body>
      <h1>Too Many Requests</h1>
      <p>I only allow 50 requests per hour to this Web site per
         logged in user. Try again soon.</p>
   </body>
</html>

Note that this specification does not define how the origin server identifies the user, nor how it counts requests. For example, an origin server that is limiting request rates can do so based upon counts of requests on a per-resource basis, across the entire server, or even among a set of servers. Likewise, it might identify the user by its authentication credentials, or a stateful cookie.

Responses with the 429 status code MUST NOT be stored by a cache.

7. Security Considerations

This document defines additional HTTP status codes. Servers can use these status codes in responses to legitimate requests as well as malicious requests. Using these status codes in a response to a malicious request does not constitute permission to forward this request on to the origin server. The security considerations for HTTP/1.1 [RFC2616] apply.
